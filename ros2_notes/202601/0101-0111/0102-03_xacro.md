# 1 使用urdl xacro建模

xacro:宏名称 调用宏，再传入不同具体的名称和参数，如：

xacro不能直接转化为urdf使用，需要通过工具**xacro**解析成URDF格式

使用urdf，xacro构建扫地机器人的模型，并使用gazebo进行仿真
1. 创建各个传感器宏之后
2. 组装环节

FishBot分多个系列，最基础的系列为拥有**两个驱动轮的差速模型**，两个驱动轮（提供驱动）和一个万向轮（提供支持和转向）

对身体的传感器、执行器的基本建模完成后，还需要给机器人添加物理属性（重量、惯性和力矩）

# 2 在Gazebo中完成机器人的仿真

gazebo所使用的模型文件为sdf格式，它继承并扩展urdf。 还有.world的世界模型，也是xml格式。

