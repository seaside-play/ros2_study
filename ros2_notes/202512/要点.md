1. 话题通信机制；
2. 自定义通信接口；
    - 接口包：需要单独独立出来，若混合有普通包，编译会失败。
        - 使用`ros2 interface show status_interfaces/msg/SystemStatus`可以查看接口包内容
        - colcon build之后也是需要source install/setup.zsh之后，才可以使用上面的命令查看接口包内容
    - 普通包
    